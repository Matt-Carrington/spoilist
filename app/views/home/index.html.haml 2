

.o-wrapper#o-wrapper
  .c-button
    %button#c-button--push-left.c-button
      XXX
  .right-content
    .row-dark
      %h3 Today
      %h1= Time.now.strftime("%A %B %d")
    .category-head
      %p Your lists
    .row
      %h2 list items: closest due date listed first
    = render 'lists'


%nav#c-menu--slide-left.c-menu.c-menu--slide-left
  %button#c-menu__close
  %ul#c-menu__items
    %li#c-menu__item
      Home
    %li#c-menu__item
      About
    %li#c-menu__item
      Services
    %li#c-menu__item
      Work
    %li#c-menu__item
      Contact

%nav#c-menu--push-left.c-menu.c-menu--push-left

:javascript

  var pushLeft = new Menu({
    wrapper: '#o-wrapper',
    type: 'push-left',
    menuOpenerClass: '.c-button',
    maskId: '#c-mask'
  });

  var pushLeftBtn = document.querySelector('#c-button--push-left');
  
  pushLeftBtn.addEventListener('click', function(e) {
    e.preventDefault;
    pushLeft.open();
  });

/ .left-menu
/   .row
/     %h2 Menu
/   .row
/     = link_to new_list_path do
/       %h2
/         New List
/   .row
/     = link_to lists_path do
/       %h2
/         New Spoil

/ .right-content
/   .row-dark
/     %h3 Today
/     %h1= Time.now.strftime("%A %B %d")
/   .category-head
/     %p Your lists
/   .row
/     %h2 list items: closest due date listed first
/   = render 'lists'